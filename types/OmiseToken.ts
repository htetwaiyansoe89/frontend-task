type OmiseToken = {
  card: {
    bank: string;
    brand: string,
    expiration_month: number,
    expiration_year: number,
    id: string,
    last_digits: string,
    name: string,
  }
  id: string,
}